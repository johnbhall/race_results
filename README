Race Results
============

I use this app to keep track of my running race results.

    # install gems
    bundle
    
    # generate the static site in development mode (auto-regenerate on save)
    stasis -d